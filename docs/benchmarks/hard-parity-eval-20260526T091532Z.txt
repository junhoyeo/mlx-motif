Grouped differential attention compares an origin stream against a noise stream, then normalizes the difference before the output projection. PolyNorm combines linear, quadratic, and cubic normalized activations. Quantized four-slot caches keep Motif decode bandwidth low when the packed q4 attention kernel consumes cache entries directly. Grouped differential attention compares an origin stream against a noise stream, then normalizes the difference before the output projection. PolyNorm combines linear, quadratic, and cubic normalized activations. Quantized four-slot caches keep Motif decode bandwidth low when the packed q4 attention kernel consumes cache entries directly. Grouped differential attention compares an origin stream against a noise stream, then normalizes the difference before the output projection. PolyNorm combines linear, quadratic, and cubic normalized activations. Quantized four-slot caches keep Motif decode bandwidth low when the packed q4 attention kernel consumes cache entries directly. Grouped differential attention compares an origin stream against a noise stream, then normalizes the difference before the output projection. PolyNorm combines linear, quadratic, and cubic normalized activations. Quantized four-slot caches keep Motif decode bandwidth low when the packed q4 attention kernel consumes cache entries directly. Grouped differential attention compares an origin stream against a noise stream, then normalizes the difference before the output projection. PolyNorm combines linear, quadratic, and cubic normalized activations. Quantized four-slot caches keep Motif decode bandwidth low when the packed q4 attention kernel consumes cache entries directly. Grouped differential attention compares an origin stream against a noise stream, then normalizes the difference before the output projection. PolyNorm combines linear, quadratic, and cubic normalized activations. Quantized four-slot caches keep Motif decode bandwidth low when the packed q4 attention kernel consumes cache entries directly. Grouped differential attention compares an origin stream against a noise stream, then normalizes the difference before the output projection. PolyNorm combines linear, quadratic, and cubic normalized activations. Quantized four-slot caches keep Motif decode bandwidth low when the packed q4 attention kernel consumes cache entries directly. Grouped differential attention compares an origin stream against a noise stream, then normalizes the difference before the output projection. PolyNorm combines linear, quadratic, and cubic normalized activations. Quantized four-slot caches keep Motif decode bandwidth low when the packed q4 attention kernel consumes cache entries directly.